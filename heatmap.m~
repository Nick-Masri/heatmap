% This is how you comment / document

function chang(decimal)
    if decimal >=5
        blue_value = ((10-decimal)/5)*255
        return (255, 0, math.floor(blue_value));
    else
        red_value = (decimal/5)*255
        return (math.floor(red_value), 0, 255)
    end